{
    "legend": "Contact (generated by contacts.muiv.json model!)",
    "fields": [
        {
            "label": "Name:",
            "model": "model.contact.name",
            "type": "text",
            "required": true
        },
        {
            "label": "Email:",
            "model": "model.contact.email",
            "type": "email",
            "required": false,
            "prefix": { "glyphicon": "envelope" }
        }
    ]
}
